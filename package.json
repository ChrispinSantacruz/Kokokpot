{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "powershell -Command \"if (Test-Path 'public') { Remove-Item 'public' -Recurse -Force }; New-Item -ItemType Directory -Name 'public'; Get-ChildItem -Path '.' -Exclude 'public','.git','node_modules' | Copy-Item -Destination 'public' -Recurse -Force\""
  }
}
